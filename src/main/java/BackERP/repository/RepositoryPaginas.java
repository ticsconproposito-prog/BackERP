package BackERP.repository;

import BackERP.models.segpaginas;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RepositoryPaginas extends JpaRepository<segpaginas, Long> {
}
